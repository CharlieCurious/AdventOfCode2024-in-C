#include <input_processor.h>
#include <stdio.h>

void get_numbers_from_line(char *input_line, unsigned int left_num, unsigned int right_num) {
    unsigned int num1, num2;

    sscanf(input_line, "%u %u", &num1, &num2);
}
